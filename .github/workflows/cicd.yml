name: cicd

on:
 workflow_dispatch:

jobs:
  code:
   runs-on: ubuntu-latest
   steps:
     - name: checkout code by git
       run: echo "code is checkout"

  build:
   runs-on: ubuntu-latest
   steps:
     - name: build is run
       run: echo "build sucessffully"
       
  test:
   runs-on: ubuntu-latest
   steps:
     - name: testing
       run: echo "approve"
       
  deploy:
   runs-on: ubuntu-latest
   steps:
     - name: deploy
       run: echo "deployed"
